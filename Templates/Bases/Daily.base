filters:
  or:
    - file.name.contains(this.file.name)
    - created.toString().contains(this.file.name)
    - start.toString().contains(this.file.name)
    - end.toString().contains(this.file.name)
    - file.links.contains(this.file)
    - last.toString().contains(this.file.name)
properties:
  file.name:
    displayName: Entry
  note.categories:
    displayName: Categories
  note.created:
    displayName: Created
  note.tags:
    displayName: Tags
views:
  - type: table
    name: Daily notes
    filters:
      and:
        - '!file.tags.contains("daily")'
        - '!file.tags.contains("monthly")'
        - file.name != this.file.name
    order:
      - file.name
      - created
      - categories
      - tags
    sort:
      - property: tags
        direction: DESC
      - property: file.name
        direction: DESC
      - property: categories
        direction: DESC
      - property: created
        direction: ASC
    columnSize:
      file.name: 250
      note.created: 142
      note.categories: 153
